# Advanced Docker Setup for a PySyft Notebook

For our docker setup we need to extend the PySyft jupyter notebook image with some dependencies and jupyter lab.

Build using

```
docker image build -t pysyft-notebook ./
```