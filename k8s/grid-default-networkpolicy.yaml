apiVersion: extensions/v1beta1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: grid-default
spec:
  ingress:
  - from:
    - podSelector:
        matchLabels:
          io.kompose.network/grid-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/grid-default: "true"
